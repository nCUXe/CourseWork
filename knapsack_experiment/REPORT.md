# Отчёт о вычислительном эксперименте
## Исследование точности приближённого алгоритма для многомерной целочисленной задачи о ранце

---

## 1. Постановка задачи

Исследуется **многомерная целочисленная задача о ранце**:

```
max cx,   x ∈ Z+^n,   Ax ≤ b
```

- `n` — число типов предметов, `m` — число ресурсных ограничений
- `A[j][i]` — расход ресурса j на одну единицу предмета i
- `b[j]` — ёмкость ресурса j,  `c[i]` — прибыль от единицы предмета i
- Переменные `x[i] ∈ {0, 1, 2, ...}` — количество единиц предмета i

Задача **NP-сложная**, поэтому практический интерес представляют приближённые алгоритмы.

---

## 2. Исследуемый алгоритм

Рассматривается алгоритм из статьи **Чиркова А.Ю. и Золотых Н.Ю.**

**Идея:** для каждого типа предмета j вычислить, сколько его можно взять при полном заполнении ранца только этим предметом, и выбрать наиболее выгодный вариант.

**Формула:**
```
u_j  = min_{i: A[i][j] > 0}  floor(b[i] / A[i][j])   — максимум единиц предмета j
j*   = argmax_j { c[j] · u_j }                        — лучший предмет
x[j*] = u_{j*},   x[i] = 0  для i ≠ j*               — решение
```

**Сложность:** O(n·m) — принципиально быстрее точных методов.

**Теоретическая гарантия (из статьи):**
```
α_mn = α_{1q} / (m + r·(α_{1q}/α_{1,q+1} − 1)),   n = m·q + r
```
При n → ∞: α_mn → 0.5914/m. Это нижняя граница — в худшем случае алгоритм даёт не менее ~59%/m от оптимума.

---

## 3. Что реализовано

| Модуль | Что делает |
|--------|-----------|
| `src/generator.py` | Генерация случайных задач с заданными характеристиками |
| `src/greedy.py` | Алгоритм из статьи (O(n·m)) |
| `src/exact.py` | Точное решение методом ДП с мемоизацией (ground truth) |
| `src/metrics.py` | Коэффициент приближения, зазор |
| `src/experiment.py` | Оркестрация серий экспериментов |
| `src/visualization.py` | Графики по результатам |

**Схема работы:**
```
config.py → 216 конфигураций
   ↓ для каждой (5 экземпляров)
generator → задача (A, b, c)
   ↓
greedy.py → приближённое решение x_G
exact.py  → оптимальное решение x_OPT  (ДП)
   ↓
ratio = profit(x_G) / profit(x_OPT) ∈ (0, 1]
   ↓
CSV + графики
```

---

## 4. Параметры эксперимента

| Параметр | Значения | Смысл |
|----------|----------|-------|
| n | 5, 6, 7, 8, 9, 10, 12 | Число типов предметов |
| m | 1, 2, 3 | Число ресурсных ограничений |
| density | dense, sparse | Плотность матрицы A |
| profit_type | random, correlated, anticorrelated | Связь прибыли с весами |
| alpha | 0.3, 0.8 | Тугость ограничений (b[j] = floor(α · ΣA[j])) |

Веса A[j][i] ∈ [1, 3] (MAX_WEIGHT=3 — держит пространство состояний ДП малым).

**Итого: 252 конфигурации × 30 экземпляров = 7560 экспериментов**

---

## 5. Результаты

### Общая статистика (7560 запусков)

| Метрика | Значение |
|---------|---------|
| Средний коэффициент приближения | **0.895** |
| Минимальный ratio | 0.324 |
| Максимальный ratio | 1.000 |
| Доля точных решений (ratio = 1.0) | **0.560** (4235/7560) |
| Всегда допустимое решение | 7560/7560 ✓ |

### Зависимость от числа ограничений m

| m | Средний ratio | Минимальный ratio | Доля точных решений |
|---|--------------|------------------|---------------------|
| 1 | **0.995** | 0.667 | **0.946** |
| 2 | 0.871 | 0.462 | 0.422 |
| 3 | 0.819 | 0.324 | 0.312 |

**Вывод:** при m=1 алгоритм почти всегда оптимален (точен в 94.6% случаев,
средний ratio 0.995). С ростом m и точность, и частота совпадения с
оптимумом резко падают — согласуется с теоретической оценкой α_mn ≈ 0.59/m.

### Зависимость от структуры данных

| Параметр | Значение | Средний ratio |
|----------|----------|--------------|
| density | dense | **0.944** |
| density | sparse | 0.846 |
| profit_type | correlated | **0.897** |
| profit_type | anticorrelated | 0.895 |
| profit_type | random | 0.893 |
| alpha | 0.3 (жёсткие) | **0.910** |
| alpha | 0.8 (мягкие) | 0.880 |

Полная таблица по подклассам (m, profit_type, density) —
`data/results/worst_case_summary.csv`.

### Лучшие подклассы (по среднему ratio, m = 1)

| m | profit_type | density | avg_ratio | min_ratio |
|---|-------------|---------|-----------|-----------|
| 1 | anticorrelated | dense | **1.000** | 0.990 |
| 1 | anticorrelated | sparse | 1.000 | 0.942 |
| 1 | random | dense | 0.998 | 0.764 |

### Худшие подклассы (наименьший минимальный ratio)

| m | profit_type | density | avg_ratio | min_ratio |
|---|-------------|---------|-----------|-----------|
| 3 | random | sparse | 0.720 | **0.324** |
| 3 | correlated | sparse | 0.767 | 0.333 |
| 3 | anticorrelated | sparse | 0.720 | 0.352 |

---

## 6. Выводы

### Найденный подкласс эффективности

**Алгоритм наиболее эффективен при m = 1 (одно ресурсное ограничение).**

- Средний ratio при m=1: **0.995** (практически оптимален)
- Доля точных решений при m=1: **0.946** (в 94.6% случаев ровно оптимум)
- Лучший подкласс — **m=1, anticorrelated**: средний ratio ≈ 1.000
  (антикоррелированная прибыль делает «один лучший предмет» почти всегда
  оптимальным)
- Причина: при одном ограничении задача одномерная, и единственный предмет взятый в максимальном количестве часто является оптимальным (смешение нескольких предметов редко даёт значительный выигрыш)

**Дополнительные факторы улучшающие ratio:**
1. **dense-матрица** (0.944 против 0.846 у sparse): каждый предмет "чувствует" все ресурсы, алгоритм лучше оценивает реальную стоимость
2. **Жёсткие ограничения (alpha=0.3)**: меньше предметов помещается, решение "одного предмета" с большей вероятностью совпадает с оптимальным

### Объяснение ухудшения при m > 1

Худший подкласс — **m=3, sparse, random**: min_ratio = 0.324,
средний ratio 0.720, точных решений лишь ~31%.

Причина: разреженная матрица создаёт предметы с разными "профилями" потребления ресурсов. Оптимальное решение выгодно использует эти профили через смесь предметов. Алгоритм же выбирает лишь один тип — именно этот случай является "adversarial" для алгоритма.

### Соответствие теории

Теорема из статьи Чиркова–Золотых предсказывает:
- Гарантированная точность α_{mn} монотонно убывает с ростом m и n
- lim α_{mn} = α_{1∞}/m ≈ 0.5914/m

На spread-графиках (`accuracy_spread_m_*.png`) построены две теоретические
линии: горизонтальная асимптота **α₁∞/m** (α₁∞ = 0.591355…) и точная
кривая **α_mn(n)** по формуле (2) Теоремы 1 статьи. Значения α_1q
вычисляются точно по рекуррентам δ_n, ε_n из статьи (`src/theory.py`)
и совпадают с её Таблицей 1 (1.0, 0.667, 0.6, 0.591549, …).

Эксперимент подтвердил тренд: avg_ratio для m=3 (~0.82) и даже минимальный
ratio (0.324) лежат заметно выше теоретической границы (α_3n → 0.197) —
ожидаемо, граница достигается лишь на специально сконструированных
задачах (прямое произведение «худших» одномерных задач из Леммы 5),
а не на случайных.

---

## 7. Запуск

```bash
pip install -r requirements.txt
python main.py
```

Результаты: `data/results/` — CSV-файлы (включая `worst_case_summary.csv`)
и графики PNG в `data/results/plots/`:
- `accuracy_spread_m_{1,2,3}.png` — разброс min..max + среднее + теор. линии;
- `accuracy_spread_m{m}_{profit_type}.png` — то же с разбивкой по типу прибыли;
- `exact_rate_by_n.png` — частота совпадения с оптимумом;
- `worst_case_by_config.png` — худший случай по подклассам;
- базовые графики avg_ratio/profit_gap.
